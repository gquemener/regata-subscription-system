<form method="post" action="{{ path('app_race_delete', {'id': race.id}) }}" hx-confirm="Are you sure you want to delete this race?">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ race.id) }}">
    <button class="btn">Delete</button>
</form>
